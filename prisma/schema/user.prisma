model User {
  id       String @id @default(cuid())
  email    String @unique
  username String @unique
  password String
  theme    Theme  @default(AUTOMATIC)
}

enum Theme {
  AUTOMATIC
  LIGHT
  DARK
}
